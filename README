Step 1: Install SQLAlchemy
If you haven't already, install SQLAlchemy along with databases which provides async support:

pip install sqlalchemy databases

Step 2: Define Database Configuration
Create a new SQLAlchemy Database instance, and define your models using SQLAlchemy ORM.

Step 3: Run the application
Run the application using Uvicorn:
uvicorn main:app --reload --port 8000